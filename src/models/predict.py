"""
predict.py — Tahmin Fonksiyonu
================================

E�itilmiş modeli yükler ve yeni veri için tahmin yapar.
Bu modül app/services/prediction_service.py tarafından kullanılır.

Sprint 4'te doldurulacak.
"""

# TODO (Sprint 4):
# - load_model(path) -> model  (cache ile)
# - load_scaler(path) -> scaler
# - predict_risk(features: dict) -> dict
#     return {
#         'prediction': 0 or 1,
#         'probability': float,
#         'risk_level': 'low' / 'medium' / 'high'
#     }
